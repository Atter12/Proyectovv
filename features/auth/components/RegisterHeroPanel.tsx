import { AuthProductHeroPanel } from "@/features/auth/components/AuthProductHeroPanel";

/** Panel derecho registro — mismo mockup dashboard que login. */
export function RegisterHeroPanel() {
  return (
    <AuthProductHeroPanel
      subtitle="Creá tu ficha en Hecom y operá Ads Holistic con control y resultados claros."
    />
  );
}
